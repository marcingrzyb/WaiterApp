<resources>

    <!-- Base application theme. -->
    <style name="Theme.WaiterLogin" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/loginPageBackgroundColor</item>
    </style>

    <style name="Theme.WaiterLogin.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.WaiterLogin.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.WaiterLogin.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


</resources>
